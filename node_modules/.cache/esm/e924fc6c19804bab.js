let arg,inquirer,syncPlayground;_198‍.x([["cli",()=>cli]]);_198‍.w("arg",[["default",["arg"],function(v){arg=v}]]);_198‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_198‍.w("./main",[["syncPlayground",["syncPlayground"],function(v){syncPlayground=v}]]);






function parseArgs(argv) {
	const args = arg({
		'--new': Boolean,
		'--replace': Boolean,
		'-n': '--new',
		'-r': '--replace'

	}, {
		argv: argv.slice(2)
	});

	return ({
		gen: args['--new'] || false,
		repl: args['--replace'] || false
	});
}

async function promptURL(options) {
	if (options.repl) {
		return ({ ...options,
			playgroundURL : await inquirer.prompt({
				type    : 'input',
				name    : 'playground_url',
				message : 'Playground URL'
			})
		});

	} else {
		return (options);
	}
}

       async function cli(args) {
//	console.log('cli()');

	let options = parseArgs(args);
	options = await promptURL(options);
//	console.log(options);
	await syncPlayground(options);
}
